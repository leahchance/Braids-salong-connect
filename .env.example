PORT=3000
MONGODB_URI=mongodb://localhost:27017/braids-salong
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random-at-least-64-characters-long
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env
# 2. Generate a strong JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 3. Replace the JWT_SECRET value with your generated secret
# 4. Update MONGODB_URI with your actual database connection string